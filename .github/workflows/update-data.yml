name: Update macro data

on:
  schedule:
    # 06:00, 15:00 KST (21:00, 06:00 UTC) - 매일 두 번 자동 갱신
    - cron: "0 21 * * *"
    - cron: "0 6 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Fetch macro data
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
        run: python macro_data_fetch.py --out data.json

      - name: Fetch Korean stock data
        run: python kr_stocks_fetch.py --out-dir kr_stocks

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json kr_stocks
          if ! git diff --cached --quiet; then
            git commit -m "chore: update macro + KR stock data ($(date -u +%Y-%m-%dT%H:%M:%SZ))"
            git push
          else
            echo "No changes"
          fi
