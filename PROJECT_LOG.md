# 프로젝트 기록 — 매크로·산업·종목 스코어카드

이 문서는 이 프로젝트를 만들면서 있었던 요청·결정·문제 해결 과정을 정리한 기록입니다.
사용법은 [README.md](README.md)를 보세요. 여기는 "왜 이렇게 만들었는지"를 남겨둔 개발 일지입니다.

## 0. 출발점

사용자가 가진 PDF 자료 `거시경제_주식추세예측체크리스트_V3.1`(MoneyTrend 제작, 교육용)을 기반으로,
업종·종목에 대한 점수를 계산해주고 판단 의견을 주는 앱을 만들어달라는 요청에서 시작했습니다.

체크리스트 구조:
- **01 매크로(40%) · 02 산업(25%) · 03 종목(25%) · 04 시장구조(10%)** 4단계, 총 30개 항목
- 항목마다 -2~+2 채점 → 단계 평균 → 가중 합산 → 총점
- 안전장치: 신용(하이일드 스프레드) 항목이 -2면 매크로 단계 점수는 -1 초과 불가(캡),
  베토 트리거 5개 중 1개 발생 시 매크로 점수 0 상한, 2개 이상이면 총점과 무관하게 노출 축소·신규진입 중단
- 총점 구간(5단계)별 실행 지침, 3단계 조합 정성 매트릭스

## 1. 1차 버전 — Claude Artifact 계산기

- HTML/CSS/JS 단일 파일로 스코어카드 계산기 제작. 30개 항목 전부 원문 체크리스트의 점수 기준을 옮겨 담음.
- Claude 아티팩트(`claude.ai/code/artifact/...`)로 게시 — 개인 비공개 링크로 즉시 사용 가능.
- **자동조회 시도**: 아티팩트는 보안 정책(CSP)상 외부 API를 직접 호출할 수 없다는 걸 확인.
  → 로컬에서 실행하는 파이썬 스크립트(`macro_data_fetch.py`)를 별도로 만들어, 그 결과 JSON을
  앱에 붙여넣거나 파일로 불러오는 방식으로 우회.

## 2. 매크로 데이터 자동조회 스크립트

무료 공개 API로 원/달러 환율, VIX, KOSPI, 미국 10년물 금리, 기대인플레이션, 연준 기준금리, 하이일드
스프레드를 가져오도록 설계. 진행 중 발견한 문제들:

- **`fred.stlouisfed.org`(FRED 비공식 CSV 엔드포인트)가 이 환경에서 계속 연결 차단됨** → 재시도 로직을
  넣어도 항상 실패. 결국 데이터 소스를 교체:
  - 실질금리 → 미국 재무부 공식 사이트(`home.treasury.gov`)
  - 연준 기준금리 → 뉴욕 연준 공식 API
  - 기대인플레이션 → 명목금리−실질금리로 직접 계산(근사치로 라벨링)
  - 미국 10년물 명목금리 → Yahoo Finance(`^TNX`). 처음에 "값÷10" 스케일링 버그가 있어서 수정함
    (Yahoo가 이미 퍼센트 그대로 반환하는데 옛날 관례대로 10으로 나눠서 0.4% 같은 엉뚱한 값이 나왔었음).
  - 하이일드 스프레드만 무료 키 없는 API가 없어 계속 "조회 실패"로 남겨둠 (3번 참고)
- **Windows 인코딩 버그 2건**:
  1. `run_macro_fetch.bat`에 넣은 한글 안내문이 Windows 콘�드 코드페이지와 충돌해서
     cmd가 깨진 텍스트를 명령어로 착각해 실행 — bat 파일 내용을 전부 영문 ASCII로 교체해서 해결.
  2. 파이썬이 stdout을 파일로 리다이렉트할 때 콘솔 코드페이지와 무관하게 시스템 기본 코드페이지(cp949)로
     인코딩해서 한글이 깨짐 — `sys.stdout.reconfigure(encoding="utf-8")`로 고정해서 해결.
- `run_macro_fetch.bat` 더블클릭 한 번으로 조회 후 클립보드에 자동 복사되게 만들어 로컬 사용 편의성 확보.

## 3. GitHub Pages로 전환 (자동화 + 공개 링크)

아티팩트 방식은 "직접 스크립트 실행 → 결과 복사 → 앱에 붙여넣기"를 매번 반복해야 해서 번거롭다는
피드백을 받고, GitHub 저장소 + GitHub Pages + GitHub Actions 조합으로 재구성:

- `index.html`을 완전한 HTML 문서(doctype/head/`<meta charset="utf-8">`)로 변경.
  → 이 과정에서 **charset 누락 버그**를 발견: 아티팩트는 발행 시 자동으로 UTF-8 래퍼를 씌워주는데,
  정적 파일로 직접 서빙하면 charset 선언이 없어 브라우저가 다른 인코딩으로 오해해 한글이 전부 깨졌음.
  로컬 HTTP 서버로 재현·검증 후 수정.
- `.github/workflows/update-data.yml` 추가 — 매일 06:00·15:00(KST) 자동으로
  `macro_data_fetch.py`를 실행해 `data.json`을 갱신·커밋.
- 페이지가 열릴 때마다 같은 저장소의 `data.json`을 `fetch()`로 자동 불러오도록 구현
  (아티팩트에서는 CSP 때문에 불가능했지만, GitHub Pages는 동일 출처라 가능).
- 파일 선택 버튼(`📂 data.json 파일 선택`)도 함께 남겨서 로컬 실행 시에도 붙여넣기 없이 바로 반영 가능.
- 저장소를 만들고 (`baeminam/stock-checklist-app`, Public), `git init` → 커밋 → GitHub push →
  Settings → Pages 활성화까지 진행. 이 계정 인증이 필요한 부분(저장소 생성, push 인증)은 사용자가 직접 처리.
- 작업 중 폴더에 있던 관련 없는 Windows 바로가기(`.lnk`) 파일이 `git add -A`에 실수로 딸려 들어가서
  별도 커밋으로 추적 해제하고 `.gitignore`에 추가.

## 4. 항목별 채점 가이드 추가

각 항목 라벨 아래 "채점 가이드 보기" 토글을 추가해, 원문 체크리스트의 시나리오→점수 표
(예: "인하+성장유지 → +2", "인하+침체대응 → -1~-2")를 펼쳐볼 수 있게 함. 30개 항목 전부에 적용.

## 5. AI 초안 채점 기능 (베타)

"종목/업종을 선택하면 AI가 대신 점수를 매겨주면 좋겠다"는 요청으로 추가:

- 사용자 본인의 API 키로 **브라우저가 직접** AI를 호출하는 방식(BYOK, bring-your-own-key).
  서버를 따로 두지 않아 구조가 단순하고, 키가 나(개발자)나 다른 방문자에게 전달되지 않음.
- 처음엔 Anthropic Claude만 넣었는데, "무료로 쓰고 싶다"는 요청을 받고
  **Google Gemini(무료 티어, 카드 등록 불필요)**를 기본값으로 추가. Anthropic은 유료 대안으로 유지.
- Gemini에서 `429 quota exceeded` 오류 발생 → 하드코딩한 모델명(`gemini-2.0-flash`)이 원인일 가능성이 높아,
  **모델명을 직접 입력/수정할 수 있게 변경**(기본값 `gemini-flash-latest`). AI 제공사는 시간이 지나며
  무료 모델 이름을 바꾸므로, 앱을 다시 고치지 않아도 사용자가 최신 모델명으로 바로 대응 가능하게 함.
- AI는 실시간 시세를 조회하지 못하므로, 항상 "초안"으로만 표시하고 검토 후 적용하는 2단계 UX
  (요청 → 30개 항목 점수·근거 테이블 확인 → 전체 적용 또는 개별 적용).
- 이후 `gemini-flash-latest`에서도 503(과부하)·429(할당량) 오류가 반복됨. 원인은 Google AI Studio의
  "비율 제한" 페이지에서 확인 — 테스트하며 여러 모델의 분당 요청 한도(RPM)를 이미 소진한 상태였고,
  일부 모델(`gemini-2.0-flash` 등)은 아예 서비스 종료(404)된 상태였음. 결국 그 페이지에서 아직 한도를
  안 쓴 모델을 찾아 `gemini-3.5-flash`로 바꾸자 정상 동작. 기본값도 이 모델로 갱신함. 이 사례는
  무료 LLM API의 모델명·가용성이 예고 없이 자주 바뀐다는 걸 보여주며, 그래서 모델명을 코드에
  하드코딩하지 않고 사용자가 바로 바꿀 수 있게 한 설계가 유효했음.

### API 키를 "GitHub에 저장"하고 싶다는 요청

- GitHub Actions Secret은 Actions 워크플로(서버) 안에서만 읽을 수 있고, 정적 페이지의 브라우저 JS는
  절대 접근할 수 없다는 점을 설명 — 이 용도로는 쓸 수 없음. 공개 저장소에 키를 직접 커밋하는 것도
  전 세계에 노출되어 도용 위험이 크므로 하지 않음.
- 여러 기기에서 반복 입력을 피하는 실제 해결책으로 **비밀번호 관리자 동기화**(추천, 무설치)와
  **별도 프록시 서버 구축**(Cloudflare Worker 등, 더 큰 작업) 두 가지를 제시 → 사용자가 비밀번호 관리자 방식 선택.

## 6. 하이일드 스프레드 자동조회 (FRED API, 진짜 GitHub Secret 활용)

하이일드 스프레드만 "조회 실패"로 남아있던 걸 사용자가 지적. 이번엔 **GitHub Secret이 실제로 맞는 용도**임을
확인 — `macro_data_fetch.py`는 브라우저가 아니라 GitHub Actions(서버)에서 돌기 때문:

- FRED(세인트루이스 연준) 공식 API(`api.stlouisfed.org`)는 무료 키 발급이 가능하고(신용카드 불필요),
  이 도메인은 (막혀있던 `fred.stlouisfed.org`와 달리) 정상적으로 접속됨을 확인.
- `FRED_API_KEY` 환경변수가 있을 때만 하이일드 스프레드를 조회하도록 구현, 없으면 기존처럼
  수동 확인 안내 메시지로 안전하게 폴백.
- `update-data.yml`에 `secrets.FRED_API_KEY`를 환경변수로 전달하도록 추가.
- 사용자가 FRED 무료 키 발급 → 저장소 Secret 등록 → 워크플로 수동 실행까지 완료,
  실제 배포된 `data.json`에서 `hy_oas_spread.ok: true` 확인 완료.

## 7. 현재 상태 (완료된 것)

| 항목 | 상태 |
|---|---|
| 30개 항목 채점 계산기 (캡·베토·가중총점·실행구간) | ✅ |
| 항목별 채점 가이드 | ✅ |
| GitHub Pages 배포 (공개 링크, 누구나 접속 가능) | ✅ `https://baeminam.github.io/stock-checklist-app/` |
| 매크로 데이터 매일 자동 갱신 (GitHub Actions) | ✅ 06:00·15:00 KST |
| 하이일드 스프레드 포함 8개 지표 전부 자동조회 | ✅ (FRED_API_KEY 등록 완료) |
| AI 초안 채점 (Gemini 무료 / Claude 유료 선택) | ✅ |
| 종목별 기록 저장·불러오기·JSON 내보내기 | ✅ (브라우저 localStorage, 방문자별 독립) |

## 8. 남겨둔 선택 사항 / 한계

- AI 초안은 실시간 데이터를 못 보므로 항상 검증 후 사용해야 함 (앱 안에도 명시).
- API 키는 브라우저별로 각자 저장됨 — 여러 기기 동기화는 비밀번호 관리자 사용 권장.
- 이 앱과 원본 체크리스트 모두 교육용 참고 자료이며 투자 권유가 아님.

## 9. 파일 구성

| 파일 | 설명 |
|---|---|
| `index.html` | 스코어카드 웹 앱 본체 (GitHub Pages 배포용, 완전한 HTML 문서) |
| `macro_data_fetch.py` | 매크로 참고 데이터를 공개 API에서 가져오는 스크립트 |
| `run_macro_fetch.bat` | 스크립트를 실행하고 결과를 클립보드에 복사하는 Windows 배치 파일 |
| `data.json` | GitHub Actions가 자동 갱신하는 매크로 데이터 |
| `.github/workflows/update-data.yml` | 매일 자동 갱신 워크플로 |
| `README.md` | 설치·사용 안내 |
| `PROJECT_LOG.md` | 이 문서 — 개발 경위 기록 |
